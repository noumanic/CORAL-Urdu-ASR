# =============================================================================
# CORAL Urdu ASR — Python Dependencies
# =============================================================================
# Covers both notebooks:
#   - urdu_asr_benchmark.ipynb  (ASR inference: Seamless, Whisper, Wav2Vec2)
#   - urdu_asr_pipeline.ipynb   (Ensemble, normalization, OOV correction, WER/CER)
#
# Python >= 3.12 required (matches Kaggle kernel used during development)
#
# GPU install (CUDA 12.1) — recommended for inference:
#   pip install torch==2.6.0+cu121 torchaudio==2.6.0+cu121 \
#       --extra-index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# CPU-only install:
#   pip install torch==2.6.0+cpu torchaudio==2.6.0+cpu \
#       --extra-index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# =============================================================================


# --- Core numeric / data ---
numpy>=2.2.0
pandas>=2.2.0

# --- Deep learning (torch + torchaudio must be installed as a matched pair) ---
# Install separately using the GPU/CPU commands above — do not pip install here
# torch==2.6.0
# torchaudio==2.6.0

# --- ASR model hub ---
# Provides: AutoProcessor, SeamlessM4Tv2Model, WhisperProcessor,
#           WhisperForConditionalGeneration, Wav2Vec2Processor, Wav2Vec2ForCTC
transformers>=4.51.0

# --- Audio loading and resampling ---
librosa>=0.11.0

# --- Progress bars ---
tqdm>=4.67.0

# --- Urdu-aware regex (supports Unicode script categories, \p{} etc.) ---
regex>=2024.11.6

# --- Fuzzy string matching (OOV correction in pipeline notebook) ---
rapidfuzz>=3.13.0